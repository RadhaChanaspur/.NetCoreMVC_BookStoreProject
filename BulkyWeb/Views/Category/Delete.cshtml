@model Category
@* in this view we are asking for category name and display order, so the model that would be used here is Category which is defined above *@


<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient mt-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-primary">Delete Category</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4" >
        <form method="post">
            @* since the method type is post, when we click on create button, it goes to controller and looks for Create view with annotation of HttpPost *@
            <div class="border p-3 mt-4">

                <div class="form-floating py-2 col-12">

                    <input asp-for="Name"  disabled class="form-control border-0 shadow" />
                    <label asp-for="Name" class="ms-2"></label>

                </div>
                <div class="form-floating py-2 col-12">

                    <input asp-for="DisplayOrder" disabled class="form-control border-0 shadow" />
                    <label asp-for="DisplayOrder" class="ms-2"></label>

                </div>


                <div class="row pt-3">
                    <div class="col-6 ">
                        <button type="submit" class="btn btn-primary form-control btn-danger">Delete</button>
                    </div>
                    <div class="col-6">
                        <a asp-controller="Category" asp-action="Index" class="btn btn-outline-primary border form-control">Back to List</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>




@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}